# ================= 核心界面与渲染 (GUI & 3D) =================
# 现代桌面应用界面框架
PyQt6>=6.4.0
# 基于 VTK 的 3D 渲染引擎 (核心绘图库)
pyvista>=0.43.0
# 连接 PyQt6 和 PyVista 的桥梁 (在窗口中显示 3D)
pyvistaqt>=0.11.0
# 虽然 pyvista 会自动安装 vtk，但为了 Nuitka 编译稳定性，建议显式声明
vtk>=9.3.0

# ================= 数据处理与科学计算 (Data Science) =================
# 矩阵与数值计算
numpy>=1.24.0
# 表格数据处理 (Excel/CSV)
pandas>=2.0.0
# 读取 Excel 文件 (.xlsx) 的引擎
openpyxl>=3.1.0
# 读取 MATLAB 文件 (.mat) 及高级科学计算
scipy>=1.10.0
# 机器学习库 (用于 PCA降维、t-SNE 流形可视化)
scikit-learn>=1.3.0

# ================= 美赛专项建模工具 (MCM Tools) =================
# 灵敏度分析库 (Sobol, Morris 等)
SALib>=1.4.7
# 复杂网络与图论 (用于网络流、拓扑结构分析)
networkx>=3.1
# 矢量绘图后端 (用于导出高质量 PDF/SVG 及辅助 2D 绘图)
matplotlib>=3.7.0

# ================= AI 与 实用工具 (AI & Utils) =================
# 调用 GPT-4o 进行代码解析与文案生成
openai>=1.0.0
# 图像处理 (用于截图、纹理处理)
Pillow>=10.0.0
# 进度条显示 (用于命令行或 UI 加载动画)
tqdm>=4.65.0
# 处理不同操作系统的路径问题
platformdirs>=3.10.0

# ================= 打包与编译 (Build) =================
# 将 Python 编译为 exe 的工具
nuitka>=2.0.0
# Nuitka 编译优化所需的压缩库
zstandard>=0.21.0