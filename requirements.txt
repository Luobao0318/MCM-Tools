# ================= GUI 与 3D 引擎 =================
# 使用 PySide6 代替 PyQt6 (Nuitka 兼容性更好)
PySide6>=6.6.0
# 3D 绘图核心
pyvista>=0.43.0
# 将 PyVista 嵌入 PySide6 的桥梁
pyvistaqt>=0.11.0
# 显式声明 VTK，防止依赖丢失
vtk>=9.3.0

# ================= 辅助库 (PyVista 依赖) =================
# 用于下载示例数据或纹理 (PyVista 可能会用到)
pooch>=1.8.0
# 图像处理 IO (处理纹理贴图)
imageio>=2.33.0
# 平台目录管理
platformdirs>=4.0.0

# ================= 数据处理 =================
numpy>=1.26.0
pandas>=2.1.0
openpyxl>=3.1.2
scipy>=1.11.0

# ================= 打包工具 =================
# 核心打包器
nuitka>=2.0.0
# Nuitka 压缩算法库
zstandard>=0.22.0